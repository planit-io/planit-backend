# 🧠 Plan-It Backend

Backend dell’app **Plan-It** — realizzato con **Quarkus + Kotlin + PostgreSQL**.

## 🚀 Setup rapido

```bash
# Clona il progetto
git clone https://github.com/<tuo-username>/triply-backend.git
cd triply-backend

# Avvia PostgreSQL e Quarkus
docker compose up -d postgres
./gradlew quarkusDev
