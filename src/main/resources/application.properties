auth.provider=keycloak  # oppure keycloak

# =====================================================
# ??  TryPly Backend - Quarkus 3.20.x LTS Configuration
# =====================================================

# --- App Metadata ---
quarkus.application.name=tryply-backend
quarkus.application.version=1.0.0
quarkus.application.description=TryPly Backend Service
quarkus.application.vendor=TryPly
quarkus.application.license=MIT
quarkus.application.license-url=https://opensource.org/licenses/MIT
quarkus.http.port=${BE_PORT:8080}

# =====================================================
# ? Database (PostgreSQL)
# =====================================================
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USER:tryply}
quarkus.datasource.password=${DB_PASSWORD:tryply}
quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/tryplydb}
quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=false
quarkus.hibernate-orm.sql-load-script=no-file

quarkus.hibernate-orm.physical-naming-strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

quarkus.flyway.migrate-at-start=true
quarkus.flyway.locations=classpath:db/migration
quarkus.flyway.schemas=public


# =====================================================
# ? Security / JWT / Keycloak
# =====================================================
mp.jwt.verify.publickey.location=${KEYCLOAK_PUBLIC_KEY_URL:http://localhost:8081/realms/tryply/protocol/openid-connect/certs}
mp.jwt.verify.issuer=${KEYCLOAK_ISSUER_URL:http://localhost:8081/realms/tryply}
quarkus.oidc.auth-server-url=${KEYCLOAK_URL:http://localhost:8081/realms/tryply}
quarkus.oidc.client-id=${KEYCLOAK_CLIENT_ID:tryply-backend}
#quarkus.oidc.credentials.secret=${KEYCLOAK_CLIENT_SECRET:}
quarkus.oidc.tls.verification=none
quarkus.oidc.roles.source=accesstoken

# consenti accesso pubblico a Swagger / OpenAPI
quarkus.http.auth.permission.swagger.paths=/q/swagger-ui,/q/swagger-ui/*,/q/openapi,/q/openapi/*
quarkus.http.auth.permission.swagger.policy=permit

# limita autenticazione alle API (modifica il prefisso se necessario)
quarkus.http.auth.permission.authenticated.paths=/*
quarkus.http.auth.permission.authenticated.policy=permit

# =====================================================
# ? OpenAPI / Swagger UI
# =====================================================
quarkus.smallrye-openapi.path=/q/openapi
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/q/swagger-ui
quarkus.swagger-ui.theme=original

# =====================================================
# ? Dev Settings
# =====================================================
quarkus.live-reload.password=dev
quarkus.devservices.enabled=${QUARKUS_DEV:true}
quarkus.log.category."org.hibernate.SQL".level=DEBUG
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n


