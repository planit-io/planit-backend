CREATE TABLE TravelEntity
(
    id          BIGINT,
    destination VARCHAR(255),
    name        VARCHAR(255),
    code        VARCHAR(255),
    startDate   VARCHAR(255),
    endDate     VARCHAR(255)
);